using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Diff.Repository.Tests
{
    [TestClass]
    public class FakeRepositoryTest
    {
        /*
        Here we could add some unit tests for Repository layer,
        but since other layers already have some demonstrations and
        the repository layer end up in a very simple class, I choose
        not add more logic to this layer just to be able to create some tests.
         */
    }
}
